import { TelephoneNumberValidatorsDirective } from './telephone-number-validators.directive';

describe('TelephoneNumberValidatorsDirective', () => {
  it('should create an instance', () => {
    const directive = new TelephoneNumberValidatorsDirective();
    expect(directive).toBeTruthy();
  });
});
